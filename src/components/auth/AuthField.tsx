import type { InputHTMLAttributes, ReactNode } from "react";
import { Eye, EyeOff, LockKeyhole } from "lucide-react";
import { useState } from "react";

type FieldProps = InputHTMLAttributes<HTMLInputElement> & { label: string; error?: string; icon?: ReactNode };
export function AuthField({ label, error, icon, className = "", ...props }: FieldProps) { return <label className="block text-sm font-medium text-slate-200"><span>{label}</span><span className="relative mt-2 block">{icon && <span className="absolute left-4 top-1/2 -translate-y-1/2 text-cyan-300">{icon}</span>}<input {...props} className={`w-full rounded-2xl border border-white/10 bg-[#111827]/85 px-4 py-3.5 text-sm text-white outline-none transition placeholder:text-slate-600 focus:border-cyan-400/60 focus:ring-4 focus:ring-cyan-400/10 ${icon ? "pl-11" : ""} ${className}`} /></span>{error && <span className="mt-2 block text-xs font-normal text-rose-300">{error}</span>}</label>; }
export function PasswordField({ label, error, ...props }: Omit<FieldProps, "type" | "icon">) { const [shown, setShown] = useState(false); return <label className="block text-sm font-medium text-slate-200"><span>{label}</span><span className="relative mt-2 block"><LockKeyhole className="absolute left-4 top-1/2 -translate-y-1/2 text-cyan-300" size={17} /><input {...props} type={shown ? "text" : "password"} className="w-full rounded-2xl border border-white/10 bg-[#111827]/85 py-3.5 pl-11 pr-12 text-sm text-white outline-none transition placeholder:text-slate-600 focus:border-cyan-400/60 focus:ring-4 focus:ring-cyan-400/10" /><button type="button" onClick={() => setShown((value) => !value)} aria-label={shown ? "Sembunyikan kata sandi" : "Tampilkan kata sandi"} className="absolute right-4 top-1/2 -translate-y-1/2 text-slate-400 transition hover:text-cyan-300">{shown ? <EyeOff size={18} /> : <Eye size={18} />}</button></span>{error && <span className="mt-2 block text-xs font-normal text-rose-300">{error}</span>}</label>; }
