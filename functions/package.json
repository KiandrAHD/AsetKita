{
  "name": "asetkita-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google/genai": "^1.16.0",
    "@google/genai": "^2.18.0",
    "asetkita": "file:..",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.2.0",
    "resend": "^4.1.2"
  },
  "devDependencies": {
    "typescript": "~5.7.2"
  }
}
